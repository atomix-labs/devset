# Build output, and what devset owns whole: its records, and the atoms' pins.
exclude = ["**/node_modules/**", "**/target/**", ".devset/**", ".config/mise/conf.d/**"]

[formatting]
column_width = 100
