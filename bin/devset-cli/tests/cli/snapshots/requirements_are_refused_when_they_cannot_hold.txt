$ devset init cycle/a --path ../cycle
error: requirements form a cycle: a → b → a
  |
  = help: drop one requirement to break the cycle
[exit 2]
$ devset init clash/both --path ../clash
error: same.toml is provided by x and y
  |
  = help: choose one in .devset/config.toml:
              [files."same.toml"]
              from = "x"   # or "y"
[exit 2]
$ devset init --path ../bad
error: `tag`, `branch`, `rev` and `path` name another source, with `git`; a profile in this one is required by name alone
 --> ../bad/profile.toml:6:5
  |
6 | x = { path = "../x" }
  |     ^^^^^^^^^^^^^^^^^
[exit 2]
$ devset init --path ../lost
error: ../lost has no profile named nowhere
  |
  = help: it holds lost; `devset list` shows them, and `devset update <source>` takes a newer commit of the source
[exit 2]
