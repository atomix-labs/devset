# Formatting is dprint's (the ruff plugin), so this is the linter only.

line-length    = 100
target-version = "py39"

[lint]
select = ["E4", "E7", "E9", "F", "I", "B", "SIM", "UP", "RUF", "PLC0415", "D102"]
