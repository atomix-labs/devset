# Checks every link into the repository, offline.
check-lychee:
    lychee --offline --no-progress --include-fragments '**/*.md'

# Checks every link, the web's too.
nightly-lychee:
    lychee --no-progress --include-fragments '**/*.md'
