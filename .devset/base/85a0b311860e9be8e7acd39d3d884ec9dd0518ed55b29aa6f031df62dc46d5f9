# Checks every link into the repository, offline, in each Markdown file git does not ignore.
check-lychee:
    lychee --offline --no-progress --include-fragments --extensions md .

# Checks every link, the web's too.
nightly-lychee:
    lychee --no-progress --include-fragments --extensions md .
